# modules to import and play with

import random, sys, os, math
# or
from random import * # don't do this, though (it's like Using)

print('hello!')
sys.exit()
print('goodbye?')

pyperclip.copy('text to copy')
pyperclip.paste() # pastes whatever is on the clipboard